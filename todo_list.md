# 活跃待办

更新时间：2026-04-05

说明：
- 本文件只保留当前尚未完成、后续仍需继续判断的事项。
- 已完成内容已从本文件移除，以避免影响后续判断。
- 已完成的历史工作请以 `git` 提交记录和 `CHANGELOG.md` 为准。

## 1. 数据库迁移补全
- 状态：`pending`
- 背景：
  - 当前已经完成受控 SQL migration、搜索索引和关键唯一约束。
  - 但全新空库的首次建表仍更依赖显式打开 `database.auto_migrate=true`，还没有完整的“空库首启 migration”方案。
- 待做：
  - 为核心业务表补充可重复执行的建表 migration
  - 明确空库初始化顺序
  - 确保新环境不需要临时开启 `auto_migrate`

## 2. VIP 真实版 Phase 1
- 状态：`pending`
- 目标：
  - 将当前 VIP 前端展示版从 mock 切到真实后端状态
- 待做：
  - 设计并落地 `user_memberships`
  - 设计并落地 `vip_usage_daily`
  - 提供当前会员状态接口
  - 提供剩余额度接口
  - 前端从 `useVIPPreview` 逐步切换到真实接口

## 3. VIP 支付与订单
- 状态：`pending`
- 目标：
  - 把 `/vip/checkout` 从展示版接入真实支付
- 待做：
  - 设计 `vip_orders`
  - 选择并接入支付渠道
  - 支付状态流转：待支付 / 支付中 / 支付成功 / 支付失败 / 退款
  - 支付回调幂等处理
  - 支付成功后自动开通会员

## 4. VIP 分析任务后端
- 状态：`pending`
- 目标：
  - 将当前 mock 异步任务替换为真实分析任务系统
- 待做：
  - 设计 `analysis_tasks`
  - 提供创建任务接口
  - 提供任务列表接口
  - 提供任务状态接口
  - 明确状态流转：
    - queued
    - running
    - completed
    - failed

## 5. VIP 报告持久化与读取
- 状态：`pending`
- 目标：
  - 将当前 mock 报告详情替换为真实报告结果
- 待做：
  - 设计 `analysis_reports`
  - 设计 `analysis_report_sources`
  - 提供报告详情接口
  - 提供来源列表接口
  - 明确报告缓存 / 复用策略

## 6. VIP 真实数据源与模型链路
- 状态：`pending`
- 目标：
  - 从展示版迈向真实的投研分析能力
- 待做：
  - 接入市场数据源
  - 接入政策信息源
  - 接入财报信息源
  - 接入新闻 / 宏观信息源
  - 设计大模型上下文构建流程
  - 生成带来源引用的结构化报告

## 7. VIP 权益与额度规则落地
- 状态：`pending`
- 当前规则：
  - 每交易日 2 次板块分析
  - 每交易日 2 次组合分析
- 待做：
  - 失败任务是否扣次
  - 同一输入命中缓存是否扣次
  - 额度按哪个交易日重置
  - 到期会员如何处理历史报告访问

## 8. 主题验收与定点修整
- 状态：`in_progress`
- 当前情况：
  - `dark` 主题最稳定
  - `cyber` 已完成两轮样式补强
  - `classic` 已完成多轮可读性修复，但仍需按页面验收式微调
- 待做：
  - 继续按页面验收 `classic`
  - 如有需要继续微调 `cyber`
  - 优先关注：
    - `/vip`
    - `/vip/checkout`
    - `/vip/tasks`
    - `/vip/reports/:id`
    - `/watchlist`
    - `/holdings`
    - `/`
