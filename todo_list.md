# 活跃待办

更新时间：2026-06-04

说明：
- 本文件只保留当前尚未完成、后续仍需继续判断的事项。
- 已完成内容已从本文件移除，以避免影响后续判断。
- 已完成的历史工作请以 `git` 提交记录和 `CHANGELOG.md` 为准。

## 全局约定与当前判断
- **缓存依赖判断：** 当前不引入 Redis。
  - 现阶段优先使用已有 PostgreSQL 快照表、数据库持久化缓存、以及进程内短 TTL 缓存。
  - 只有在出现多后端实例、跨进程共享缓存、分布式锁、任务队列、统一限流或高频热点接口明显压垮数据库时，才重新评估 Redis。
- **新增依赖约束：** 未明确进入生产化改造前，不新增 Redis / MQ / LLM SDK 等基础依赖；先把边界、数据表、失败降级和验收样本定义清楚。
- **全量基金目录同步与不存在基金治理：** 2026-06-01 已重新执行 `go run ./cmd/crawler --list all --save-db --catalog-only`。本次 Eastmoney 返回 26,927 条目录；先标记本地 `catalog_missing=111`，扫描确认无用户自选、观察分组、持仓、交易流水或 `fund_valuation_profiles` 引用后，已备份到 `.omx/backups/manual/catalog-missing-funds-cleanup-20260601-094512.json` 并清理。当前本地 `funds` 表 26,927 条，`stock_holdings` 为 4,533 条；目录状态为 `active=26,737`、`unavailable=190`、`catalog_missing=0`。该模式只更新基金目录元数据、`catalog_status`、`catalog_synced_at` 与 `updated_at`，不覆盖已抓取的基金经理、公司、净值、规模和持仓；默认搜索只返回 `active`，名称含“(后端)”的不可用份额不会污染搜索结果；全量详情/持仓抓取仍属于长任务，后续应分批执行。
- **生产访问恢复：** 2026-06-02 已处理 `http://fund.wrenzeal.top/` 无法进入的问题。根因是公网 443 未监听，浏览器/访问环境自动升级 HTTPS 后连接被拒；同时发现前端 PM2 未带 `BACKEND_URL` 会让 Next.js 代理回退 `127.0.0.1:8080`，导致 `/health` 误报 500。当前线上已启用 Let's Encrypt HTTPS，Nginx 将 `/api/` 与 `/health` 直连后端 `127.0.0.1:13896`；生产后端配置已放到 `/etc/fund-live/fundlive.yaml` 并通过 systemd `FUNDLIVE_CONFIG` 读取；部署脚本和 workflow 默认端口已收口。后续如果更换后端端口，必须同步 Nginx、`BACKEND_URL`、backend healthcheck 与 self-hosted workflow。
- **前端依赖安全基线：** 2026-06-02 已将 `web` 依赖审计修复到 `npm audit = 0 vulnerabilities`。当前 `next` / `eslint-config-next` 为 `16.2.7`，并通过 `overrides.postcss=8.5.15` 覆盖 Next 仍固定声明的 `postcss@8.4.31`；不要按 npm audit 的旧建议降级到 Next 9，因为该项目依赖 Next 16 App Router。
- **后端 Go 版本基线：** 2026-06-02 已将 `go.mod` 升级到 `go 1.26.3`，并按 Go 1.26 release notes 扫描受影响点；当前无需修改后端业务代码。CI 会继续通过 `go-version-file: go.mod` 读取版本，生产部署机需要安装 Go 1.26.3 或兼容 1.26 系列工具链。
- **登录安全基线：** 2026-06-04 已完成后端登录/注册/Google 登录安全加固。当前认证入口具备进程内失败限流（默认 15 分钟窗口：密码 5 次、注册 8 次、Google 10 次）、10 位字母+数字且无空白的注册密码策略、16KB JSON body 限制、登录错误信息收敛、Google 邮箱 claim 格式校验，以及生产 `auth.cookie_secure=true`。该限流仅适合当前单后端实例；若后续扩到多实例或出现明显暴力攻击流量，需要评估共享限流存储、Nginx/WAF rate limit 或 Redis。

---

## 0. 基金量化分析系统（含事件模块与建议分布）
- 状态：`in_progress`
- 当前阶段：**P0 / P1 已完成，P2a / P2b / P2c 第一轮均已完成；2026-05-18 已完成运行态样本复核与 baseline_v3 规则收口；后续进入真实 provider 评估**
- 当前产品口径补充：
  - **事件层主轴优先围绕“当前持仓 + 持仓股票当前表现/时事热点 + 关联行业/主题影响”构建**
  - **高可信分析的主要来源应是：基金当前实际持仓股票、持仓权重、所在行业/主题、近期有来源的公告/新闻/政策/宏观热点与行情表现**
  - **基金本身事件只做辅助信号，默认不应占太高权重**：基金公告、基金经理、规模变化、费率、限购/分红等只在直接影响基金产品结构、流动性、风险暴露或交易体验时提升优先级
  - **若基金跟踪的是指数 / ETF，则优先看目标 ETF / 指数产品的实际持仓结构、重仓股事件与行业/主题事件，而不是单独强调指数规则层事件**
  - **上一季持仓变化只作为辅助观察，不作为主结论核心**；当前判断仍以最新可用持仓与近期事件为主
  - **AI 不是可信度修复手段**：AI 解释层只能基于证据包提升可读性，不改写规则评分、建议分布或风险等级
- 当前聚焦说明：
  - `todo_list.md` 中其余与量化看板无关的事项先不纳入本轮实施判断
  - 当前只继续推进“基金量化分析 / 量化看板”相关后续工作
- 待做拆解（按优先级）：
  - P2a｜可信度增强与证据链收口（AI 前置）
    - 当前进展：P2a 第一轮已完成；2026-05-18 已完成 6 个核心样本运行态复核，补充 baseline_v3 规则收口：加仓需 `increase >= 55%`、减仓需 `decrease >= 60%`，否则默认观察；反方证据不再把正向事件塞进“适合观察”的反方列表；审计命令会优先标记低可信度因子，避免总分看似可用但底层数据不足。
    - [x] 拆细现有 `confidence`：至少区分持仓新鲜度、持仓覆盖率、实时行情可用性、事件来源强度、热点关联强度、历史/上一季对比完整度
    - [x] 建立“证据包 / evidence bundle”结构：每条结论都要能回溯到持仓、行业/主题快照、公告/新闻/政策/宏观事件、行情表现或历史持仓对比中的一个或多个来源
    - [x] 调整分析权重口径：当前持仓股票 + 行业/主题 + 近期热点应作为主权重；基金自身公告/产品事件默认作为低权重辅助项，仅在经理变更、清盘/限购、重大费率变化、异常规模/赎回等直接影响基金产品的事件上升权重
    - [x] 为持仓股票事件加入“权重 × 新鲜度 × 来源强度 × 影响方向 × 影响周期”评分，避免低权重小票或泛泛公告抢占主结论
    - [x] 为行业/主题热点加入映射规则：热点必须先落到行业/主题或具体持仓股票，再按基金暴露权重影响结论；不能只因基金名称或宽泛关键词就给强判断
    - [x] 增加反方理由：每个偏积极 / 偏谨慎结论都要同时展示削弱该结论的风险或数据缺口，例如持仓过旧、热点无直接持仓映射、行业涨幅已高、事件方向混杂等
    - [x] 优化前端表达：把“偏加仓 / 偏减仓”这类强投资建议口吻弱化为“当前结构偏积极 / 当前更适合观察 / 风险暴露偏高”，并明确这是量化观察，不是交易指令
    - [x] 核心样本复核：对 `159813 / 012970 / 023408 / 005827 / 000362 / 000370` 逐只输出“主证据、反方证据、数据缺口、可信度扣分原因、是否符合直觉”的审计记录
  - P2b｜AI 解释层
    - 当前进展：P2b 边界第一轮已完成；当前仍不接真实 LLM SDK，默认返回无 AI / 降级证据摘要。真实 provider 接入前需继续保持“不改评分、只解释证据”的边界。
    - [x] 定义 `AIExplanationService` / provider 边界：输入只能是规则型 `FundAnalysis`、证据包、结构化事件、基金/持仓/板块主题快照和可引用来源；输出只包含解释、归因、摘要、风险提示和引用，不写回评分字段
    - [x] AI 只负责解释、归因、摘要，不直接决定 `increase / hold / decrease` 建议百分比、`total_score` 或 `risk_level`
    - [x] AI 必须基于证据包生成内容；对没有来源支撑的热点、行业判断或投资结论必须拒绝生成或降级为“无法确认”
    - [x] 增加无 AI / AI 超时 / AI 失败时的降级文案：页面仍展示规则型评分和结构化事件，不阻塞核心分析链路
    - [x] 设计带来源引用的解释输出结构：必须能追溯到已有 `event_impacts`、持仓、行业/主题快照或公告来源，不允许生成无法归因的投资结论
    - [x] 先写核心样本验收清单：`159813 / 012970 / 023408 / 005827 / 000362 / 000370` 的 AI 文案不得改变规则建议，只能增强可读性；当前由 `ai_explanation.rule_recommendation`、`boundary_notice`、引用校验和审计命令输出承担验收入口
  - P2c｜事件 / 分析 / 解释缓存收口
    - 当前进展：P2c 第一轮已完成；当前仍不引入 Redis，先复用 `fund_analysis_snapshots` + 进程短 TTL + 历史持仓 PostgreSQL 快照。
    - [x] 先复用现有 `fund_analysis_snapshots`，补充解释层缓存字段或新建轻量解释快照表，避免每次详情页都重新触发多源解释
      - 已补：`ai_explanation.cache_key/cache_status/expires_at/invalidation_basis`；`/fund/:id/analysis` 改为 fresh snapshot-first；batch/rankings 过滤过期快照；dashboard 支持 `include_analysis=false`，前端详情/首页避免 dashboard 重复构建分析。
    - [x] 对公告 / 新闻热点 / 宏观 / 政策 / 指数 / 历史持仓事件继续优先做 PostgreSQL 持久化快照或短 TTL 进程缓存；当前不引入 Redis
      - 当前状态：持仓公告与基金公告已有进程短 TTL；历史持仓已有 PostgreSQL 快照；宏观/政策/指数事件为本地轻量种子/规则生成，暂不需要 Redis。
    - [x] 明确缓存失效策略：按交易日、公告发布时间、热点事件时间、基金持仓报告期和分析版本失效，而不是靠手工清理
      - 已补：分析快照要求当前 `analysis_version`、同一上海自然日、未过 `expires_at` 且解释层 cache key 匹配；解释层 cache key 包含基金、分析版本、分析类型、交易日、持仓披露期、规则结论与证据签名。
- 当前执行顺序建议：
  1. **P2a 已完成：可信度增强、证据链、反方理由和核心样本复核**
  2. **P2b 已完成第一轮：AI 解释层边界、引用校验与降级策略**
  3. **P2c 已完成第一轮：解释层 / 事件层缓存快照收口；2026-05-18 已将分析版本推进到 `baseline_v3` 以失效旧快照**
  4. **2026-05-29 前端看板与排行榜重构已完成，并扩展到主要页面滚动进入动效统一：`/analysis/[fundId]` 已改为结论证据优先的分段布局，`/analysis/rankings` 已改为首屏概览 + 三类观察池 + 首位样本 + 分榜列表；2026-06-01 已按视觉反馈保留上方积极池/观察池/风险池统计卡并列展示，并把下方“结构偏积极 / 最值得观察 / 高风险关注”分榜改为上下排布；新增 `scroll-reveal.tsx` 后，首页/持仓/自选/想法/公告/VIP/详情类长页均已接入区块或列表卡片渐显；下一步仍是接真实 provider 或继续做样本校准**
  5. **2026-05-29 基金人工分类标签覆盖层第一版已完成：管理员可维护主分类、主板块、主主题、人工标签与备注；Dashboard 返回 `classification_override`，前端持仓分类卡展示人工校正但保留自动权重拆解**
  6. **2026-06-01 持仓总览补齐总价值与总收益：官方口径按已就绪本金，盘中口径按已确认份额，新增收益率说明，避免只显示今日盈亏而缺少累计收益视角**
  7. **2026-06-01 持仓页信息架构已收敛为工作台导航：总览/记录/持仓/风险/流水/工具六块分区展示，默认不再一屏堆满全部模块；顶部快捷按钮明确“记一笔 / 看持仓 / 查风险 / 看流水”**
  8. **2026-06-02 自选页分组管理下拉交互已修复：目标分组触发器和下拉面板使用不透明主题背景，菜单通过 portal 固定弹层渲染，避免选项与下方按钮重叠时点击穿透；分组选择下方的基金搜索结果也改为 portal/fixed 浮层下拉，不再撑高整块 div**
  9. **2026-06-02 前端依赖漏洞已修复：`next` / `eslint-config-next` 升级到 `16.2.7`，`postcss` 通过 npm overrides 固定到 `8.5.15`，`npm audit` 已为 0 漏洞**
  10. **2026-06-02 后端 Go 版本基线已升级到 `1.26.3`：`go.mod`、README 与 CHANGELOG 已同步；Go 1.26 兼容扫描未发现需要业务代码迁移的 API 用法**
  11. **2026-06-02 自选页“编辑分组”按钮点击无反应已修复：排序拖拽入口收敛为独立手柄，编辑按钮不再被整卡 draggable 抢占，并补充按钮点击态与编辑弹窗进入动画**
  12. **2026-06-03 浏览器标签页 favicon 已更新：使用透明背景蓝色线性活跃度图标，SVG 为主图标、ICO 为兼容 fallback**
  13. **2026-06-03 浏览器标签页标题已调整为 `FundLive - 你的基金估值系统`，并优化为更高频、更慢节奏的长标题滚动显示；首页左上角主标题保持“涨了多少”，副标题改为 `FundLive - 实时基金估值`**
  14. **2026-06-03 前后端代码优化已完成：前端品牌块收口到共享 `BrandMark`，后端基金搜索归一化与排序字段预计算收口，保持接口与业务语义不变**
  15. **2026-06-04 后端登录与鉴权安全加固已完成：登录/注册/Google 登录失败限流、密码强度策略、认证错误信息收敛、认证 body 大小限制、Google 邮箱 claim 校验、本机反代信任收口与生产 Secure Cookie 已落地；后续多实例再评估共享限流**
- 2026-05-29 新增实施方案｜**基金人工分类标签覆盖层**
  - 状态：`completed`（第一版已落地）
  - 背景判断：现有分类主要来自基金类型关键词、持仓股票行业/主题映射和少量 `fund_classification_overrides`，对 ETF 联接、QDII、宽基/行业混合主题、名称与持仓不一致的基金仍容易出现主分类或主板块偏差；需要允许管理员人工填写主分类、主板块、主主题和自定义标签，但不能把人工标签伪装成自动持仓权重。
  - 本轮边界：
    - [x] 后端扩展 `fund_classification_overrides`：补齐 `primary_theme_code`、`manual_tags_json`，保留现有 `category_code / primary_sector_code / sector_tags_json` 兼容字段。
    - [x] 后端新增管理员 API：读取 / 保存单只基金人工分类覆盖，并提供分类、板块、主题候选字典；保存时校验字典 code，记录 `updated_by / note`。
    - [x] Dashboard 返回 `classification_override`，前端可以展示“人工校正”来源；自动行业/主题 breakdown 权重仍按持仓聚合展示，不把人工标签写成权重。
    - [x] 前端在持仓分类卡中增加管理员编辑入口：支持选择主分类、主板块、主主题，填写自定义标签和备注；普通用户只看到人工校正结果和标签，不看到编辑表单。
    - [x] 若人工主板块 / 主主题存在，展示层优先显示人工主标签，同时保留下方自动权重拆解，并提示“权重仍来自自动持仓聚合”。
  - 验收：
    - [x] 管理员可对任意基金保存 / 更新人工分类与标签，刷新后仍能回显。
    - [x] 非管理员不会暴露编辑入口。
    - [x] 自动分类低覆盖时仍展示覆盖提示，人工校正与自动置信度不互相混淆。
    - [x] Go 相关测试、前端 lint/build、`git diff --check` 通过。
- “做到何种程度算完成”：
  - **P0 事件层继续增强算完成：**
    - 持仓事件、板块/主题事件、基金自身/季报变化三类都已有稳定展示
    - ETF / 联接基金 / 跟踪指数基金在事件层的口径仍以**实际持仓**为主，不退回到单纯指数规则解释
    - 详情页 / 排行榜 / 持仓页 / 自选页在已有量化标签和入口下，不出现明显“事件层有内容但前端展示不到”断层
  - **P2a 可信度增强算完成：**
    - 每个主要结论都能展示主证据、反方证据和可信度扣分原因
    - 持仓股票、行业/主题与近期热点的证据权重明显高于基金自身泛公告或产品说明
    - 低来源强度、低持仓映射强度或过旧数据不会生成强结论
    - 核心样本审计结果可复核，且明显不符合直觉的样本有校准记录
  - **整个量化增强阶段算完成：**
    - 结构化评分结果可复现
    - ETF / 联接基金 / 主动基金 / QDII 的事件解释口径清晰
    - 前端页面能稳定展示总分、建议分布、模块分、主要事件、主证据、反方证据、可信度说明与 AI 解释层状态/引用边界
    - 基金详情页只展示量化摘要，不堆叠完整判定规则；完整证据、事件、可信度与方法限制集中到 `/analysis/[fundId]`
    - 量化详情页已按“结论总览 → 建议分布 → 六维模块可视化 → 主/反证据 → 事件链 → 风险/结构/行情辅助”重排，减少纯文字说明
    - 排行榜 / 持仓页 / 自选页 / 详情页已统一复用分析结果
    - 后续接 AI 只增强表达和可读性，不需要重写核心评分链路

## 1. 海外数据源选型建议文档
- 状态：`in_progress`
- 当前阶段：**VIP / 付费 API 先不做；已完成官方资料初筛与短期固定海外源配置化，后续仅在确认采购/授权后接正式 provider**
- 产物：[`docs/overseas-data-source-selection.md`](docs/overseas-data-source-selection.md)
- 目标：
  - 为 QDII / 海外股票后续的分时图、正式生产环境实时估值和更稳的行情链路预留选型结论
  - 明确当前临时可用方案与后续可升级路径
- 当前建议：
  - **短期：** 继续保留当前固定海外源，先满足海外实时估值；固定海外源已独立配置为 `quote.overseas_source` / `QUOTE_OVERSEAS_SOURCE`，默认 `tencent`，支持切换 `sina` 做兜底或对照
  - **中期首选：** 优先评估 `Polygon / Massive`，官方股票 WebSocket 覆盖分钟聚合、逐笔成交和 NBBO quotes，最贴近 QDII 分时图 + 实时估值
  - **中期备选：** `Alpaca Market Data`、`Twelve Data`；Alpaca 适合后续扩展券商/Broker 生态，Twelve Data 适合多市场统一格式快速接入
  - **长期企业级：** 如需更正式授权与采购流程，可评估 `Intrinio`

| 数据源 | 适合阶段 | 美股实时行情 | 历史/分时能力 | WebSocket | 接入复杂度 | 成本预期 | 对 FundLive 的适配度 | 当前建议 |
|---|---|---|---|---|---|---|---|---|
| 当前固定海外源（Tencent / Sina） | 短期过渡 | 有 | 弱/不稳定 | 无公开稳定授权方案 | 低 | 低 | 适合先把 QDII 实时估值跑通；现已配置化 | 继续保留，默认 Tencent |
| Polygon / Massive | 中期首选 | 强 | 强 | 强 | 中 | 中-高 | 最适合后续做海外分时图与稳定实时估值 | **优先评估** |
| Alpaca Market Data | 中期备选 | 强，但需区分 IEX / SIP / delayed SIP | 强 | 强 | 中 | 中 | 若后续考虑券商/Broker 生态，扩展性好 | 可评估 |
| Twelve Data | 中期轻量备选 | 较强 | 较强，官方页面说明支持 1min 至多周期 | 有 | 低-中 | 中 | 适合快速接入多市场，但需核对正式稳定性与限制 | 可评估 |
| Intrinio | 长期企业级 | 强 | 强 | 有实时产品体系 | 中-高 | 高 | 更偏正式企业采购与授权清晰路线 | 长期备选 |

- 当前已完成：
  - [x] 明确本轮不接 VIP / 付费官方 API，只做 provider 边界和文档
  - [x] 官方资料初筛：Polygon / Alpaca / Twelve Data / Intrinio 均具备可用于中期评估的实时或分时能力，但正式实时、SIP/全市场覆盖、再分发和缓存需要逐项确认授权
  - [x] QDII 固定海外行情源配置化：`quote.overseas_source` / `QUOTE_OVERSEAS_SOURCE`，默认 `tencent`，支持 `sina`
- 后续评估点：
  - 是否支持美股**分钟级数据**，足够支撑 QDII 分时图
  - 是否有明确的**实时授权/再分发限制**
  - 免费层 / 付费层对实时数据是否有延迟，尤其是 IEX、SIP、delayed SIP、NBBO quote 的差异
  - 是否支持 REST + WebSocket 双形态
  - 是否便于给当前 `FundLive` 增加“正式海外源 + 当前固定海外源兜底”双路策略
  - 采购前必须确认 API key 管理、限流、缓存时长、页面展示授权和用户再分发边界

## 2. VIP 真实数据源与模型链路
- 状态：`pending`
- 目标：
  - 从“后端真实骨架 + 模板化内容”迈向真实投研能力
- 待做：
  - 接入市场数据源
  - 接入政策信息源
  - 接入财报信息源
  - 接入新闻 / 宏观信息源
  - 将当前模板化异步状态替换为真实任务调度 / worker
  - 设计大模型上下文构建流程
  - 生成带来源引用的结构化报告

## 3. VIP 权益与额度规则落地
- 状态：`pending`
- 当前规则：
  - 每交易日 2 次板块分析
  - 每交易日 2 次组合分析
- 待做：
  - 失败任务是否扣次
  - 同一输入命中缓存是否扣次
  - 额度按哪个交易日重置
  - 到期会员如何处理历史报告访问

## 4. VIP 支付生产化与渠道扩展
- 状态：`pending`
- 当前情况：
  - 已支持微信支付 `Native` 下单、查单与回调，开发环境保留预览开通后备入口
- 待做：
  - 明确正式商户资质与进件方案（普通商户 / 服务商 / 聚合支付）
  - 评估支付宝或第三方聚合支付接入可行性
  - 补齐退款 / 售后 / 关单策略
  - 明确支付失败、订单过期和人工补单处理流程

## 5. VIP 页面主题验收与定点修整
- 状态：`in_progress`
- 当前情况：
  - `dark` 主题最稳定
  - `cyber` 已完成两轮样式补强
  - 非 VIP 页面（`/`、`/watchlist`、`/holdings`、`/issues`、`/announcements`）已完成主题收尾
  - `classic` 在 VIP 页面仍需按页面验收式微调
  - 2026-05-25 已完成 `classic` 首轮统一样式收口：VIP 主标题渐变、Hero 光斑、主 CTA、持仓/自选 VIP 入口按钮在浅色主题下改为更稳的金色/蓝色浅底样式，并修正 VIP 介绍页 / 开通页局部 JSX 缩进
- 待做：
  - 有浏览器截图或人工验收条件时，继续按页面复核 `classic`
  - 如有需要继续微调 `cyber`
  - 优先关注：
    - `/vip`
    - `/vip/checkout`
    - `/vip/tasks`
    - `/vip/reports/:id`

## 6. 持仓页前端体验增强（排序 / 筛选 / 未补齐过滤）
- 状态：`completed`（增强版第一轮）
- 当前进展：
  - [x] 已完成待补齐过滤、高频仓位/盈亏排序，以及通用本地排序/筛选面板
  - [x] 2026-06-01 已完成持仓总览总价值 / 总收益补齐：总收益按当前总价值减已覆盖本金计算，随官方口径 / 盘中预估切换同步更新，并展示收益率
  - [x] 2026-06-01 已完成持仓页复杂度收口：新增工作台式功能导航，总览/记录/持仓/风险/流水/工具分区展示；长说明默认压缩，导入与 VIP 归入工具区，流水筛选归入流水区
  - [x] 排序支持总本金/金额、官方/预估盈亏、官方/预估涨跌幅、分笔数、最近录入、量化建议与风险等级；筛选支持盈利/亏损、就绪/部分未就绪、单笔/多笔，并可和“只看待补齐”叠加
  - [x] 2026-05-25 完成持仓记录体验第一轮重构：新增引导式 `HoldingRecordComposer`，把原来的大表单改为“30 秒记录一笔”流程卡；无持仓空态改为解释记录后的收益，已有持仓才展示总览指标
  - [x] 2026-05-25 完成持仓记录体验第二轮：已有持仓基金支持一键复记/补仓，备注增加常用标签，本次记录增加提交前摘要；金额提交前统一去除千分位逗号并校验正数
  - [x] 2026-05-25 已完成“持仓校正 / 修改”能力：当系统确认份额、确认净值、本金或备注与支付宝 / 微信等外部平台显示不一致时，用户可以直接校正当前持仓，不需要通过买入 / 卖出绕路
  - [x] 2026-05-25 已完成“持仓对账 / 组合体检”第一轮：对比持仓本金与“份额 × 确认净值”的差异，提示需复核 / 小差异 / 待补齐 / 已对齐；组合层补充仓位集中度、量化高风险、待补齐、平台校正、主要盈亏来源等体检信号
  - [x] 2026-05-25 已完成“交易流水 / 最近活动”第一轮：新增买入、校正、删除活动流水表与后端查询接口，持仓页展示最近账本时间线，当前持仓快照与活动痕迹开始拆分
  - [x] 2026-05-25 已完成“卖出 / 减仓”第一轮：分笔持仓支持部分减仓，后端同步减少当前持仓快照并写入 `sell` 流水，最近活动时间线可展示卖出记录
  - [x] 2026-05-25 已完成“真实清仓”第一轮：卖出面板支持全部清仓，后端写入 `sell` 清仓流水并移除当前持仓快照，和删除错误记录区分开
  - [x] 2026-05-25 已完成“流水作废 / 冲正”第一轮：历史流水支持非物理作废标记，最近活动可展示“已作废”和作废原因；第一轮不自动回滚当前持仓快照，避免误冲正直接影响资产口径
  - [x] 2026-05-26 已完成“最近账本筛选”第一轮：流水查询支持 `fund_id / type / voided / limit` 过滤；持仓页最近账本支持按基金、流水类型、有效/已作废筛选，并补充筛选空态与清空入口
  - [x] 2026-05-26 已完成“平台来源 / 校正来源结构化”第一轮：校正时可选择支付宝 / 微信 / 天天基金 / 银行 / 手工 / 其他来源；后端校验并写入 `source_platform / source_label`，当前持仓和流水保留来源字段，最近账本支持按来源筛选并展示来源标签
  - [x] 2026-05-27 已完成“流水高级筛选 / 展开更多”第一轮：流水查询支持关键词、开始/结束日期与分页取数；持仓页最近账本增加关键词、日期范围筛选和“加载更多”入口，便于按备注、来源、作废原因追溯历史记录
  - [x] 2026-05-27 已完成“流水作废 / 冲正预览”第二轮第一版：按流水类型提供只读回滚影响预览，展示本金、份额、确认净值、确认日等可能需要人工校正的字段；当前不自动修改持仓快照，只引导用户先作废流水、再手动校正
  - [x] 2026-05-27 已完成“持仓剩余模块收口”第一版：独立流水详情页、对账筛选/差异历史、组合行业主题洞察、批量导入预览入口、页面内持仓提醒面板均已落地
  - [x] 2026-05-27 已完成“持仓增强项收口”第一版：低风险流水支持回滚预览后自动冲正并作废原流水；批量导入支持预览后直接写库；组合洞察优先基于基金 Top10 持仓穿透识别重仓股重叠；提醒面板支持已读 / 稍后提醒本地持久化
- 目标：
  - 在已经完成“按基金聚合 / 分笔明细 + 官方口径 / 盘中预估”切换的基础上，继续提升持仓页的可用性
  - 让用户更容易快速定位高风险持仓、重仓持仓、以及仍未补齐确认净值/份额的记录
  - 降低首次记录心理负担，让用户明确知道“记录后能自动得到什么”

### A. 按基金聚合视图的排序与筛选
- 状态：`completed`
- 当前进展：
  - [x] 新增本地排序 / 筛选面板，覆盖总本金、盈亏、涨跌幅、分笔数、最近录入、盈利/亏损、就绪状态和单笔/多笔
  - [x] 同一套控制同时作用于按基金聚合与分笔明细，默认仍保持原始顺序且不主动筛选
- 目标：
  - 让聚合视图不只是“能看”，而是“能快速定位”
- 建议能力：
  - 排序项：
    - 按总本金
    - 按官方盈亏
    - 按预估盈亏
    - 按官方涨跌幅
    - 按预估涨跌幅
    - 按分笔数
    - 按最近录入时间
  - 筛选项：
    - 只看盈利 / 只看亏损
    - 只看已完整就绪
    - 只看部分就绪
    - 只看单笔基金 / 多笔基金
- 实现建议：
  - 前端先做本地排序 / 本地筛选
  - 基于 `holdingAggregates` 和已拉取的预估聚合结果进行派生，不急着新增后端接口
  - 排序和筛选状态保存在页面组件本地 state
- 默认建议：
  - 默认排序先保持“录入顺序 / 最近操作顺序”
  - 不默认加筛选，避免用户一进来就误以为数据丢失

### B. 聚合卡片增加“按盈亏 / 仓位排序”能力
- 状态：`completed`
- 当前进展：
  - [x] 新增“仓位 / 金额最大优先”“亏损最多优先”“盈利最多优先”排序入口
  - [x] 盈亏排序会跟随当前 `官方口径 / 盘中预估` 切换；缺失排序值统一排到列表后部
- 目标：
  - 直接解决“我想先看最亏的 / 仓位最大的”这个高频诉求
- 建议首批排序入口：
  - 仓位从高到低（按总本金）
  - 官方盈亏从高到低 / 从低到高
  - 盘中预估盈亏从高到低 / 从低到高
- 交互建议：
  - 在聚合视图顶部放一个“排序方式”下拉
  - 常用选项前置：
    - 仓位最大优先
    - 亏损最多优先
    - 盈利最多优先
  - 当前排序方式要可见，避免用户忘记页面为何变了
- 数据口径要求：
  - 若当前处于“官方口径”，排序默认基于官方字段
  - 若当前处于“盘中预估”，排序默认基于预估字段
  - 对缺失字段的聚合卡片，统一排到列表后部

### C. “只看未补齐记录”过滤器
- 状态：`completed`
- 当前进展：
  - [x] 新增“只看待补齐”过滤器，聚合视图按 `metrics_scope` / `incomplete_holdings_count` / `real_metrics_ready` 过滤，分笔视图按 `shares` / `confirmed_nav_date` / `real_metrics_ready` 过滤
  - [x] 过滤开启后展示筛选说明、命中数量和无命中空态，可一键恢复全部持仓
- 目标：
  - 方便用户快速排查哪些记录还没有确认份额、没有最新官方净值，或仍处于部分就绪状态
- 建议过滤维度：
  - 分笔视图：
    - 只看未确认份额
    - 只看待同步今日官方净值
    - 只看真实口径未就绪
  - 聚合视图：
    - 只看部分就绪基金
    - 只看完全未就绪基金
- 判定口径建议：
  - 分笔：
    - `!shares` 或 `!confirmed_nav_date` -> 待补确认份额
    - `real_metrics_ready = false` -> 真实口径未就绪
  - 聚合：
    - `metrics_scope = partial` -> 部分就绪
    - `metrics_scope = none` -> 完全未就绪
- 交互建议：
  - 做成一个显式筛选 toggle：
    - `只看待补齐`
  - 开启后顶部说明文案同步变化，例如：
    - “当前仅显示待确认份额或待同步官方净值的记录”

### 实施顺序建议
1. ~~先做 **只看未补齐记录** 过滤器~~（已完成）
   - 成本最低，排查价值最高
2. ~~再做 **聚合卡片按盈亏 / 仓位排序**~~（已完成）
   - 高频使用场景最明确
3. ~~最后补全 **聚合视图通用排序 / 筛选面板**~~（已完成）
   - 作为更完整的交互层收尾

### 验证要求
- 排序：
  - 同一批聚合数据在不同排序方式下顺序正确
  - 官方 / 预估口径切换后，排序依据同步变化
- 过滤：
  - “只看待补齐”开启后，只展示符合条件的数据
  - 关闭后恢复完整列表
  - 聚合与分笔两个视图下过滤结果都正确
- 交互：
  - 排序 / 过滤切换不会破坏当前视图模式与口径模式
  - 切换后顶部总览和列表语义仍一致


### F. 持仓校正 / 修改能力
- 状态：`completed`
- 背景：
  - 当前用户只能新增买入记录或删除记录，实际使用中支付宝 / 微信等平台展示的份额、确认净值、确认日期、持仓本金可能和系统自动补齐结果有细微差异
  - 这种差异不一定代表真实买入 / 卖出，更多是净值确认、四舍五入、手续费、历史迁移口径不同，需要“校正当前持仓”而不是新增交易流水
- 首批能力：
  - [x] 分笔持仓行提供“校正”入口
  - [x] 支持修改持仓本金、备注、申购日期
  - [x] 支持手动校正确认份额、确认净值、确认净值日；三项需要成组填写，避免产生半套口径
  - [x] 支持清空手动确认口径，回到系统按官方历史净值自动确认
  - [x] 后端接口校验用户归属，不能修改其他用户持仓
- 交互原则：
  - 基金代码不在校正里修改；选错基金时应删除后重新记录
  - 校正入口只出现在持仓行内，和“买入 / 补仓”入口区分
  - 持仓列表展示“已按外部平台校正”轻提示，完整规则和数值仍放在展开校正面板里
- 验收标准：
  - 修改后持仓列表、聚合本金、份额、市值 / 盈亏口径会刷新
  - 手动校正后的持仓不会被夜间自动净值补齐任务覆盖
  - 清空手动份额 / 净值 / 日期后，可重新走系统自动确认链路


### G. 持仓对账体验
- 状态：`completed`
- 目标：
  - 让用户知道“为什么系统和支付宝 / 微信显示不一致”，而不是只提供一个校正入口
  - 把口径差异拆成可解释的待处理项，降低用户对数据可信度的疑虑
- 当前已完成：
  - [x] 展示系统持仓本金与“确认份额 × 确认净值”的差异
  - [x] 按差异状态标记：需复核 / 小差异 / 待补齐 / 已对齐
  - [x] 对已手动校正记录展示“外部校正”提示
  - [x] 给出常见原因说明：四舍五入、手续费、确认日期、分红、平台迁移口径
  - [x] 增加“只看需复核 / 已校正”筛选器
  - [x] 支持用户记录校正来源：支付宝 / 微信 / 天天基金 / 银行 App / 手工迁移 / 其他，并在对账面板和持仓行展示最近校正来源
  - [x] 增加差异历史第一版：基于校正流水展示最近几次校正前后值，并可进入流水详情继续查看完整影响链

### H. 组合级体检与洞察
- 状态：`completed`
- 目标：
  - 让持仓页不只是列表，而是能一眼看出组合是否集中、是否有高风险、今日主要波动来自哪里
- 当前已完成：
  - [x] 组合体检分数与状态文案
  - [x] 单只基金仓位过高提醒
  - [x] 前三仓位集中提醒
  - [x] 量化高风险 / 风险偏高信号提醒
  - [x] 待补齐与平台校正记录提醒
  - [x] 今日主要贡献 / 拖累来源提示
  - [x] 行业 / 主题集中度：基于持仓基金的行业/主题快照按本金加权聚合
  - [x] 重仓股重叠度增强：优先基于基金 Top10 持仓明细穿透识别多基金重复股票；量化事件 `related_symbols` 作为缺少完整 Top10 数据时的补充信号
  - [x] 同类基金重复暴露提醒：按主行业/主题识别多只基金集中在同一主线
  - [x] 组合风格画像：成长 / 红利 / 医药 / 科技 / 海外等

### I. 交易流水与当前持仓拆分
- 状态：`completed`
- 目标：
  - 明确区分“买入 / 卖出 / 分红 / 份额调整 / 平台校正”和“当前持仓快照”
  - 避免校正污染真实交易历史，也避免买卖记录无法追溯
- 当前已完成：
  - [x] 设计并落地首版交易流水表与领域模型：`tb_user_fund_holding_transaction` / `UserFundHoldingTransaction`
  - [x] 买入 / 补仓、持仓校正、删除记录会写入活动流水，保留当时金额、份额、确认净值、确认日、交易时间和备注快照
  - [x] 新增最近活动查询接口，并在持仓页展示最近账本时间线
  - [x] 支持卖出 / 减仓第一轮：当前先支持部分减仓，按金额或确认份额减少持仓快照，并写入 `sell` 流水
  - [x] 支持真实清仓第一轮：使用 `sell_all` 写入清仓流水，并移除当前持仓快照，避免用户用“删除”表达真实赎回
  - [x] 当前仍采用“持仓快照 + 活动流水”混合维护，避免在分红/冲正口径未完整前强行改为纯流水聚合
  - [x] 支持作废 / 冲正第一轮：`tb_user_fund_holding_transaction` 增加 `voided / voided_at / void_reason`，后端校验只能作废当前用户自己的未作废流水；前端最近活动提供作废入口并灰显已作废记录
  - [x] 2026-05-26 已完成“分红 / 红利再投、份额调整”第一轮：后端支持现金分红只写流水、红利再投增加当前份额、份额调整按目标份额或差额更新快照；前端持仓行提供分红与份额调整入口，最近账本覆盖对应类型
- 待做：
  - [x] 平台来源 / 校正来源结构化第一轮：流水和当前持仓记录 `source_platform / source_label`，校正面板提供来源选择，最近账本支持来源筛选；暂不做自动导入
  - [x] 补齐分红 / 红利再投、份额调整类型的业务入口与校验
  - [x] 自动冲正第一版：低风险流水可在回滚预览后应用自动冲正；系统会作废原流水、按安全规则更新当前持仓快照并保留冲正痕迹；有后续有效流水或快照不可安全恢复时仍只做人工校正建议
  - [x] 增加最近账本筛选第一轮：按基金、流水类型、有效/已作废过滤
  - [x] 增加流水高级筛选第一轮：按时间范围、备注关键词过滤，并支持更多历史记录展开
  - [x] 独立流水详情页：展示单条流水完整字段、metadata、关联持仓快照、只读回滚预览与后续影响链
  - [x] 已完成当前阶段口径评估：继续保留“快照为主、流水追溯”的混合模式；纯流水聚合会等分红、冲正自动应用、批量导入等高风险口径更稳定后再重新评估

### J. 批量导入与平台模板
- 状态：`completed`（增强版第一轮）
- 目标：
  - 不直接抓取支付宝 / 微信，先做稳定可控的手工导入
- 当前已完成：
  - [x] CSV 模板导入第一版：提供示例模板与 CSV / 中文逗号 / Tab 粘贴解析
  - [x] 批量粘贴导入第一版：最多预览 20 行，并支持把有效行带入上方“记录一笔”入口逐笔确认
  - [x] 批量导入写库第一版：CSV / 粘贴预览后可将有效行直接批量写入，每行复用单笔记录校验与流水链路，单行失败不影响其他行
  - [x] 支付宝 / 微信 / 天天基金 / 银行 / 手工迁移字段模板入口
  - [x] 导入前预览与错误行提示
- 边界：
  - 不直接抓取支付宝 / 微信等平台；批量写入仅处理用户确认粘贴的有效行，当前最多 20 行，失败行需人工修正后重试

### K. 持仓提醒系统
- 状态：`completed`（页面内增强版第一轮）
- 目标：
  - 从“用户主动看”升级为“关键变化主动提示”
- 当前已完成：
  - [x] 官方净值 / 份额未补齐提醒
  - [x] 单只持仓跌幅超过阈值提醒
  - [x] 量化风险升高 / 风险偏高提醒
  - [x] 校正记录长期未复核提醒
  - [x] 重仓行业 / 主题出现事件信号提醒
  - [x] 提醒已读 / 稍后提醒第一版：页面内提醒支持本地持久化隐藏、24 小时稍后提醒和恢复全部提醒
- 后台推送、短信、邮件、平台自动抓取、纯流水聚合仍不做；当前继续保持“快照为主、流水追溯”的混合模式。
- 边界：
  - 第一版为持仓页内提醒面板，不做站内信、短信、邮件或后台推送；已读 / 稍后提醒仅本机持久化，不跨设备同步
