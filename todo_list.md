# 活跃待办

更新时间：2026-04-27

说明：
- 本文件只保留当前尚未完成、后续仍需继续判断的事项。
- 已完成内容已从本文件移除，以避免影响后续判断。
- 已完成的历史工作请以 `git` 提交记录和 `CHANGELOG.md` 为准。

---

## 0. 基金量化分析系统（含事件模块与建议分布）
- 状态：`in_progress`
- 当前阶段：**P0 / P1 已完成，进入 P2（AI 解释层）**
- 当前产品口径补充：
  - **事件层主轴优先围绕“当前持仓 + 持仓股票当前表现/时事 + 关联板块/主题影响”构建**
  - **若基金跟踪的是指数 / ETF，则优先看其实际持仓结构与对应持仓事件，而不是单独强调指数规则层事件**
  - **上一季持仓变化只作为辅助观察，不作为主结论核心**
- 当前聚焦说明：
  - `todo_list.md` 中其余与量化看板无关的事项先不纳入本轮实施判断
  - 当前只继续推进“基金量化分析 / 量化看板”相关后续工作
- 待做拆解（按优先级）：
  - P2｜AI 解释层
    - [ ] 在规则型评分稳定后再接 AI 解释
    - [ ] AI 只负责解释、归因、摘要，不直接决定建议百分比
- 当前执行顺序建议：
  1. **先做 P2：AI 解释层**
- “做到何种程度算完成”：
  - **P0 事件层继续增强算完成：**
    - 持仓事件、板块/主题事件、基金自身/季报变化三类都已有稳定展示
    - ETF / 联接基金 / 跟踪指数基金在事件层的口径仍以**实际持仓**为主，不退回到单纯指数规则解释
    - 详情页 / 排行榜 / 持仓页 / 自选页在已有量化标签和入口下，不出现明显“事件层有内容但前端展示不到”断层
  - **整个量化增强阶段算完成：**
    - 结构化评分结果可复现
    - ETF / 联接基金 / 主动基金 / QDII 的事件解释口径清晰
    - 前端页面能稳定展示总分、建议分布、模块分与主要事件
    - 排行榜 / 持仓页 / 自选页 / 详情页已统一复用分析结果
    - 后续若接 AI，不需要重写核心评分链路

## 1. 海外数据源选型建议文档
- 状态：`pending`
- 目标：
  - 为 QDII / 海外股票后续的分时图、正式生产环境实时估值和更稳的行情链路预留选型结论
  - 明确当前临时可用方案与后续可升级路径
- 当前建议：
  - **短期：** 继续保留当前固定海外源，先满足海外实时估值
  - **中期首选：** 优先评估 `Polygon`
  - **中期备选：** `Alpaca Market Data`、`Twelve Data`
  - **长期企业级：** 如需更正式授权与采购流程，可评估 `Intrinio`

| 数据源 | 适合阶段 | 美股实时行情 | 历史/分时能力 | WebSocket | 接入复杂度 | 成本预期 | 对 FundLive 的适配度 | 当前建议 |
|---|---|---|---|---|---|---|---|---|
| 当前固定海外源 | 短期过渡 | 有 | 弱/不稳定 | 无公开稳定方案 | 低 | 低 | 适合先把 QDII 实时估值跑通 | 继续保留 |
| Polygon | 中期首选 | 强 | 强 | 强 | 中 | 中-高 | 最适合后续做海外分时图与稳定实时估值 | **优先评估** |
| Alpaca Market Data | 中期备选 | 强 | 强 | 强 | 中 | 中 | 若后续考虑券商/Broker 生态，扩展性好 | 可评估 |
| Twelve Data | 中期轻量备选 | 较强 | 较强 | 有 | 低-中 | 中 | 适合快速接入多市场，但需核对正式稳定性与限制 | 可评估 |
| Intrinio | 长期企业级 | 强 | 强 | 强 | 中-高 | 高 | 更偏正式企业采购与授权清晰路线 | 长期备选 |

- 后续评估点：
  - 是否支持美股**分钟级数据**，足够支撑 QDII 分时图
  - 是否有明确的**实时授权/再分发限制**
  - 免费层 / 付费层对实时数据是否有延迟
  - 是否支持 REST + WebSocket 双形态
  - 是否便于给当前 `FundLive` 增加“固定海外源 + 备用海外源”双路兜底

## 2. VIP 真实数据源与模型链路
- 状态：`pending`
- 目标：
  - 从“后端真实骨架 + 模板化内容”迈向真实投研能力
- 待做：
  - 接入市场数据源
  - 接入政策信息源
  - 接入财报信息源
  - 接入新闻 / 宏观信息源
  - 将当前模板化异步状态替换为真实任务调度 / worker
  - 设计大模型上下文构建流程
  - 生成带来源引用的结构化报告

## 3. VIP 权益与额度规则落地
- 状态：`pending`
- 当前规则：
  - 每交易日 2 次板块分析
  - 每交易日 2 次组合分析
- 待做：
  - 失败任务是否扣次
  - 同一输入命中缓存是否扣次
  - 额度按哪个交易日重置
  - 到期会员如何处理历史报告访问

## 4. VIP 支付生产化与渠道扩展
- 状态：`pending`
- 当前情况：
  - 已支持微信支付 `Native` 下单、查单与回调，开发环境保留预览开通后备入口
- 待做：
  - 明确正式商户资质与进件方案（普通商户 / 服务商 / 聚合支付）
  - 评估支付宝或第三方聚合支付接入可行性
  - 补齐退款 / 售后 / 关单策略
  - 明确支付失败、订单过期和人工补单处理流程

## 5. VIP 页面主题验收与定点修整
- 状态：`in_progress`
- 当前情况：
  - `dark` 主题最稳定
  - `cyber` 已完成两轮样式补强
  - 非 VIP 页面（`/`、`/watchlist`、`/holdings`、`/issues`、`/announcements`）已完成主题收尾
  - `classic` 在 VIP 页面仍需按页面验收式微调
- 待做：
  - 继续按页面验收 `classic`
  - 如有需要继续微调 `cyber`
  - 优先关注：
    - `/vip`
    - `/vip/checkout`
    - `/vip/tasks`
    - `/vip/reports/:id`

## 6. 持仓页前端体验增强（排序 / 筛选 / 未补齐过滤）
- 状态：`pending`
- 目标：
  - 在已经完成“按基金聚合 / 分笔明细 + 官方口径 / 盘中预估”切换的基础上，继续提升持仓页的可用性
  - 让用户更容易快速定位高风险持仓、重仓持仓、以及仍未补齐确认净值/份额的记录

### A. 按基金聚合视图的排序与筛选
- 目标：
  - 让聚合视图不只是“能看”，而是“能快速定位”
- 建议能力：
  - 排序项：
    - 按总本金
    - 按官方盈亏
    - 按预估盈亏
    - 按官方涨跌幅
    - 按预估涨跌幅
    - 按分笔数
    - 按最近录入时间
  - 筛选项：
    - 只看盈利 / 只看亏损
    - 只看已完整就绪
    - 只看部分就绪
    - 只看单笔基金 / 多笔基金
- 实现建议：
  - 前端先做本地排序 / 本地筛选
  - 基于 `holdingAggregates` 和已拉取的预估聚合结果进行派生，不急着新增后端接口
  - 排序和筛选状态保存在页面组件本地 state
- 默认建议：
  - 默认排序先保持“录入顺序 / 最近操作顺序”
  - 不默认加筛选，避免用户一进来就误以为数据丢失

### B. 聚合卡片增加“按盈亏 / 仓位排序”能力
- 目标：
  - 直接解决“我想先看最亏的 / 仓位最大的”这个高频诉求
- 建议首批排序入口：
  - 仓位从高到低（按总本金）
  - 官方盈亏从高到低 / 从低到高
  - 盘中预估盈亏从高到低 / 从低到高
- 交互建议：
  - 在聚合视图顶部放一个“排序方式”下拉
  - 常用选项前置：
    - 仓位最大优先
    - 亏损最多优先
    - 盈利最多优先
  - 当前排序方式要可见，避免用户忘记页面为何变了
- 数据口径要求：
  - 若当前处于“官方口径”，排序默认基于官方字段
  - 若当前处于“盘中预估”，排序默认基于预估字段
  - 对缺失字段的聚合卡片，统一排到列表后部

### C. “只看未补齐记录”过滤器
- 目标：
  - 方便用户快速排查哪些记录还没有确认份额、没有最新官方净值，或仍处于部分就绪状态
- 建议过滤维度：
  - 分笔视图：
    - 只看未确认份额
    - 只看待同步今日官方净值
    - 只看真实口径未就绪
  - 聚合视图：
    - 只看部分就绪基金
    - 只看完全未就绪基金
- 判定口径建议：
  - 分笔：
    - `!shares` 或 `!confirmed_nav_date` -> 待补确认份额
    - `real_metrics_ready = false` -> 真实口径未就绪
  - 聚合：
    - `metrics_scope = partial` -> 部分就绪
    - `metrics_scope = none` -> 完全未就绪
- 交互建议：
  - 做成一个显式筛选 toggle：
    - `只看待补齐`
  - 开启后顶部说明文案同步变化，例如：
    - “当前仅显示待确认份额或待同步官方净值的记录”

### 实施顺序建议
1. 先做 **只看未补齐记录** 过滤器
   - 成本最低，排查价值最高
2. 再做 **聚合卡片按盈亏 / 仓位排序**
   - 高频使用场景最明确
3. 最后补全 **聚合视图通用排序 / 筛选面板**
   - 作为更完整的交互层收尾

### 验证要求
- 排序：
  - 同一批聚合数据在不同排序方式下顺序正确
  - 官方 / 预估口径切换后，排序依据同步变化
- 过滤：
  - “只看待补齐”开启后，只展示符合条件的数据
  - 关闭后恢复完整列表
  - 聚合与分笔两个视图下过滤结果都正确
- 交互：
  - 排序 / 过滤切换不会破坏当前视图模式与口径模式
  - 切换后顶部总览和列表语义仍一致
